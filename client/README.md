# Adaptive English — Client

This is a minimal Vite + React (JSX) client for the adaptive learning server.

Quick start:

1. cd into `client`
2. Run `npm install`
3. Run `npm run dev`

The client expects the server to run at `http://localhost:4000`.
